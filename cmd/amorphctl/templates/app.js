// myapp — browser-side helpers.
//
// Reactive logic should generally live in MBL watchers (see setup.mbl
// and auth/*.mbl). This file is for browser-only helpers such as
// custom DOM events or formatting utilities. It is intentionally
// empty — add code as the application grows.
