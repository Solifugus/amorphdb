output("Starting corrected high-volume write operations")

counter = 0
while counter < 50:
    output("Writing entry", counter)
    counter = counter + 1

output("High-volume write operations completed")
output("Total entries written:", counter)