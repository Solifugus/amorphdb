# Moved

Project context for Claude Code lives in **`CLAUDE.md` at the repository root**,
not here. This file is a pointer so the duplicate is not recreated.

A near-copy previously lived at this path and drifted out of sync with the root
version — each ended up newer than the other in different places, which is worse
than having no second copy at all. They were reconciled on 2026-07-30 and the
content now lives in one place.

If you are working inside `docs/` and your tooling loaded this file: read
`../CLAUDE.md` instead.
